<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="true" />
    <!-- Prevent transitive dependencies from excluding fixed-orientation,
         cellular-only, or camera-less vehicles from the AAOS track. -->
    <uses-feature android:name="android.hardware.screen.landscape" android:required="false" />
    <uses-feature android:name="android.hardware.screen.portrait" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Normal (non-runtime) permission used only for brief sleep-timer reset
         feedback on Automotive devices that expose a vibrator. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Phone-only storage libraries contribute these transitively. The AAOS
         artifact uses app-private files and persisted content URIs instead. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />

    <application
        android:allowBackup="false"
        android:appCategory="audio"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false" >

        <!-- Declare ShelfDrive as an Android Automotive OS media app so OEM
             launchers (Polestar / Volvo / reference AAOS) render the home
             tile as a media-control tile with transport controls.
             NOTE: this is the AAOS metadata `com.android.automotive` — NOT the
             Android Auto `com.google.android.gms.car.application`. Play rejects
             an AAB that declares the automotive uses-feature AND the Android
             Auto metadata together; a native AAOS app must use only this. -->
        <meta-data
            android:name="com.android.automotive"
            android:resource="@xml/automotive_app_desc" />
        <meta-data
            android:name="androidx.car.app.TintableAttributionIcon"
            android:resource="@drawable/abs_book_1" />

        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/settings_activity_title"
            android:theme="@style/SettingsTheme" >
            <!-- Not distraction-optimized: this activity has free-text inputs
                 (server URL / username / password) which AAOS Driver Distraction
                 Guidelines prohibit while the vehicle is in motion. AAOS will
                 auto-hide this activity while moving; users must be parked. -->
            <meta-data
                android:name="distractionOptimized"
                android:value="false" />
            <intent-filter>
                <action android:name="android.intent.action.APPLICATION_PREFERENCES" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <receiver android:name="androidx.media.session.MediaButtonReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <service
            android:name=".player.PlayerNotificationService"
            android:enabled="true"
            android:exported="true" >
            <!-- Exposed so OEM Car Media template hosts can bind and call
                 onGetRoot / onLoadChildren. Caller packages are still gated
                 inside PlayerNotificationService.isValid(). -->
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- AAOS requires authenticated media apps to integrate with Android's
             AccountManager. Interactive sign-in remains in parked settings;
             this service exposes lifecycle management and no auth tokens. -->
        <service
            android:name=".accounts.ShelfDriveAuthenticatorService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_ACCOUNT_AUTHENTICATOR">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>
            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/shelfdrive_account_authenticator" />
        </service>

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:grantUriPermissions="true"
          android:exported="false">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
